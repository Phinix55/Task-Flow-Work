// Since AppColors didn't define darkRoseSolid previously explicitly as names, we will use the palette.
// Adding them here to correct the missing constants.
// Wait, I will just patch lib/app/theme/app_colors.dart properly later, but for now I'll just use the existing constants.
